import { name, version } from '../../package.json'

export const PROJECT_NAME = name
export const PROJECT_VERSION = version
export const PROJECT_GITHUB_REPO = 'https://github.com/jkcorrea/teleprompt'

export const FILE_EXT = '.prompt'
export const FILE_EXT_REGEX = /\.prompt$/
